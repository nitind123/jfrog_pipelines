resources:
  - name: gcp_packer
    type: GitRepo
    configuration:
      path: deepikasl/packer-gcp
      gitProvider: deepikaGithub

pipelines:
  - name: bash_pipeline
    steps:
      - name: gcp
        type: Bash
        configuration:
          inputResources:
            - name: gcp_packer
          integrations:
            - name: gcp
        execution:
          onExecute:
            - echo "Executing testBash.."
            - printenv
            
