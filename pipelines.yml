

pipelines:
  - name: demo_manual
    configuration:
       affinityGroup: deepika
    steps:
      
      - name: jenkins_test
        type: Jenkins
        configuration:
#           affinityGroup: deepika
          inputSteps:
            - name: manual_trigger
          jenkinsJobName: Pipe-Performace
          integrations:
            - name: deepikaJenkins
          
